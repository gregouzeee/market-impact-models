# S3 Storage Configuration for SSP Cloud
# =======================================
#
# These variables are automatically set by SSP Cloud.
# You only need this file if you want to test S3 integration locally.
#
# On SSP Cloud:
#   - All variables below are automatically configured
#   - Data is automatically synced to/from S3
#   - No manual configuration needed
#
# For local development:
#   - Copy this file to .env
#   - Fill in your S3 credentials (if testing S3 locally)
#   - Otherwise, leave it empty (local files will be used)

# S3 Endpoint URL (MinIO compatible)
# On SSP Cloud: automatically set to https://minio.lab.sspcloud.fr
S3_ENDPOINT_URL=https://minio.lab.sspcloud.fr

# AWS credentials (SSP Cloud provides these automatically)
AWS_ACCESS_KEY_ID=your_access_key_here
AWS_SECRET_ACCESS_KEY=your_secret_key_here

# Optional: Session token (SSP Cloud may use this)
# AWS_SESSION_TOKEN=your_session_token_here

# S3 Bucket name (your personal bucket on SSP Cloud)
# Example: username-bucket or project-specific-bucket
S3_BUCKET_NAME=your_bucket_name_here

# AWS Region (usually us-east-1 for MinIO)
AWS_DEFAULT_REGION=us-east-1


# Notes:
# ------
# 1. On SSP Cloud, you don't need to set these - they're automatic
# 2. Locally, if you don't set these, the code uses local files only
# 3. To find your SSP Cloud bucket name: check your SSP Cloud service settings
# 4. Data structure in S3:
#    - market-impact-data/orderbook/*.json    (order book snapshots)
#    - market-impact-data/results/*.json      (calibrated parameters)